interface:
  display_name: "Multi-Repo Review"
  short_description: "Review one or more local git repos from committed diffs only."
  default_prompt: "Use repo-review. Review one or more local repositories using committed diffs only, read the root guidance files first, default to HEAD~1..HEAD when no range is provided, parallelize repo inspection when possible, and report only concrete findings with file references."
