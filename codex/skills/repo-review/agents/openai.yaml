interface:
  display_name: "Multi-Repo Review"
  short_description: "Low-bias committed-diff review with optional safe autofix."
  default_prompt: "Use $repo-review to review a local repository with immutable committed ranges and fresh-context subagents."
